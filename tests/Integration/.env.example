# Copy to .env if you need to override compose interpolation later.
# Default credentials are for a disposable local stack only.

LSAR_WP_URL=http://127.0.0.1:8088
LSAR_WP_TITLE=Local SEO Integration
LSAR_ADMIN_USER=admin
LSAR_ADMIN_PASSWORD=admin
LSAR_ADMIN_EMAIL=integration@example.test
